doctype html

include ../components

block variables

html(lang="ru")
  head
    title= title
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    meta(name="description", content="АМР «Артмедиа» - реклама на радио, LED Экранах, мониторах в г. Арзамас. Изготовление аудиороликов и видеороликов.")
    <link rel="apple-touch-icon" sizes="180x180" href="favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="favicon/favicon-16x16.png">
    <link rel="manifest" href="../favicon/site.webmanifest", crossorigin="use-credentials">
    <link rel="mask-icon" href="favicon/safari-pinned-tab.svg" color="#fe8a00">
    <meta name="msapplication-TileColor" content="#fe8a00">
    <meta name="theme-color" content="#ffffff">
    //- генерируйте favicon тут - https://realfavicongenerator.net/
    //- в подключение фавикон манифеста обязательно добавьте строчки crossorigin="use-credentials"

    //- используйте только для шрифта находящегося на первом экране
    link(rel="preload", href="fonts/futurabc.woff2", as="font", crossorigin="anonymous")
    link(rel="preload", href="fonts/futurabc.woff", as="font", crossorigin="anonymous")
    link(rel="preload", href="fonts/gilroy-bold.woff2", as="font", crossorigin="anonymous")
    link(rel="preload", href="fonts/gilroy-bold.woff", as="font", crossorigin="anonymous")
    link(rel="stylesheet", href="css/style.min.css")

  body
    +sprite()
    .wrapper(class=wrapperMods)
      if (hasHeader)
        +header(headerMods, currentUrl)

      block main

      if (hasFooter)
        +footer(footerMods)

    block modals

    script(src="js/vendor.min.js")
    script(src="js/main.min.js")
